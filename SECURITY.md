# 安全策略

## 支持版本

当前项目处于早期版本，安全修复以 `main` 分支为准。

## 敏感信息

以下内容禁止提交到 Git：

- `.env`
- 数据库备份
- 服务器密码
- GitHub Token
- 企业微信 Secret、Token、EncodingAESKey
- TLS 私钥和证书
- 员工真实敏感数据导出文件

## 生产建议

- 上线后立即修改默认管理员密码。
- 使用随机长字符串作为 `JWT_SECRET_KEY`。
- 生产环境启用 HTTPS。
- 企业微信自建应用按最小可见范围授权。
- 定期备份数据库，并限制备份文件访问权限。
- 饭堂核销账号只授予 `verifier` 角色。
- 行政查看账号只授予 `auditor` 角色。

## 漏洞反馈

如果发现安全问题，请不要在公开 Issue 中披露细节。建议通过私有渠道联系维护者，并提供：

- 影响版本或 commit
- 复现步骤
- 影响范围
- 建议修复方式
